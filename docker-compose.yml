services:
  db_students:
    image: postgres
    container_name: db_students
    restart: always
    ports:
      - 6000:5432
    env_file:
      - .env
    volumes:
      - pg:/var/lib/postgresql/data
    
  students_app_node:
    container_name: students_app_node
    build: .
    ports:
      - 5001:5000
    env_file:
      - .env
    depends_on:
      - db_students

volumes:
  pg: